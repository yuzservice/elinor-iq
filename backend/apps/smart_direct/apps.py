from django.apps import AppConfig


class SmartDirectConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.smart_direct"
    label = "smart_direct"
    verbose_name = "Smart Direct"
